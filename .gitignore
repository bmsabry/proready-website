# Agent Zero files
.a0proj/
AgentZero_Migration_Package.tar.gz

# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local env
.env
.env.local

# Build output. Cloudflare Pages runs `npm run build` and publishes the dist/
# it produces, so committed build artifacts are never served — they only go
# stale and mislead. Keep them out of the repo.
dist/
dist-ssr/

