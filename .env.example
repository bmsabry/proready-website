# Copy to .env.local for local dev, or set in Cloudflare Pages env vars for prod.
#
# Leave VITE_API_BASE empty to keep the form in simulated-success mode
# (useful while backend is down). Set it to the backend URL to go live.

# VITE_API_BASE=http://127.0.0.1:8000
# VITE_API_BASE=https://proreadyengineer-training-api.onrender.com
VITE_API_BASE=
